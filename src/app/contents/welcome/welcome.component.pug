.jumbotron
  .container
    h1 {{data.title}}
    section
      p
        strong([colorify]="data.greetingColor") {{data.greeting}}
      p
        span 当前日期：
        time {{currDateStr|mydate:'yyyyMMdd':'zh'}}
